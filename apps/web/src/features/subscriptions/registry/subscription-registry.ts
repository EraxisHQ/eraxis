// import type { Subscription } from "../types/subscription";

// export const SUBSCRIPTION_REGISTRY: Subscription[] = [];

import { DEFAULT_SUBSCRIPTIONS } from "../constants/default-subscriptions";

export const SUBSCRIPTION_REGISTRY = DEFAULT_SUBSCRIPTIONS;
