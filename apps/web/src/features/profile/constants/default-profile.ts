import type {
  Profile,
} from "../types/profile";

export const DEFAULT_PROFILE:
  Profile = {
  userId: "1",

  displayName:
    "Eraxis Admin",

  email:
    "admin@eraxis.com",

  avatarUrl: null,
};