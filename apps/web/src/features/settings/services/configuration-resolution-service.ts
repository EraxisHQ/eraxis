/**
 * =====================================
 * Eraxis
 * Module: Configuration Resolution
 *
 * Purpose:
 * Resolves configuration values.
 *
 * Milestone:
 * M001-09-05
 * =====================================
 */

export function resolveConfiguration(
  values: string[]
) {

  return values.at(-1);
}
